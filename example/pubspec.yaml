name: drift_sync_core_example
description: Minimal usage example for drift_sync_core.
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  drift_sync_core:
    path: ..

dev_dependencies:
  lints: ^4.0.0
