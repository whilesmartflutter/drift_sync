# Changelog

## 0.1.0

Initial release.

* `SyncTriggers` — single trigger surface for sync: app lifecycle,
  internet connectivity, and an optional periodic timer, behind one
  `attach` / `detach` pair. Mutable interval via `setInterval` for
  remote-config integration.
